# 更新日志

2026/04/14：开始着手计划

2026/04/15：确定方案&gpt沟通

2026/04/16：建库，导入数据，从最简单的随机推送开始（慢慢堆栈）

2026/04/29：使用TRAE加速构建，完成反馈系统

- 搭建简易推送系统，导入到mysql数据库（本地self_system库self_system_message表并插入1062条数据）

2026/06/25：项目重构（v2.0）

- 新增 **X-Token 鉴权**：所有业务接口统一通过请求头 `X-Token: dancehole` 鉴权，缺/错一律返回 401
- 推送接口兼容 **Hitokoto 规范**：
  - 请求参数：`c`（句子类型）、`min_length`（默认 0）、`max_length`（默认 30）、`encode`（`text`/`json`）
  - 返回字段：`id / content / type / from / from_who / created_at / length`
- 数据库新增 `from_who` 字段与索引；并补充完整的 `init.sql`（含建库 + 注释 + 升级 SQL）
- 新增接口：`/add`（新增语录）
- 修复 `cool_factor` 在 `intensity=0` 时的除零风险
- 新增 `docs/` 目录：
  - `docs/API.md` —— 完整接口文档
  - `docs/DEPLOY.md` —— 部署文档（开发 / waitress / gunicorn / systemd）
  - `docs/DATABASE.md` —— 数据库结构 + 导入 / 导出 / 备份恢复文档
- 前端 (`fontend/index.html`) 同步支持 `X-Token` 鉴权，可选自定义 token
